{
  "db": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_DATABASE}",
    "password": "${DB_PASSWORD}",
    "name": "db",
    "connector": "postgresql",
    "user": "${DB_USER}"
  }
}
