{
  "db": {
    "host": "${DB_HOST || 'localhost'}",
    "port": "${DB_PORT || 5432}",
    "database": "${DB_DATABASE || 'management_database'}",
    "password": "${DB_PASSWORD || 'postgres'}",
    "name": "db",
    "connector": "postgresql",
    "user": "${DB_USER || 'postgres'}"
  }
}
