{
  "db": {
    "name": "db",
    "connector": "postgresql",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_DATABASE}",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}"
  }
}
